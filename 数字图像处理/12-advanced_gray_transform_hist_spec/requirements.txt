# 高级灰度变换与直方图规定化实验依赖
# 安装命令: pip install -r requirements.txt

numpy>=1.24,<3.0
matplotlib>=3.7,<4.0
Pillow>=10.0,<12.0

# 可选：网络不可用时加载 skimage 内置样本图像作为回退
scikit-image>=0.22,<1.0

# 建议：用于在 VS Code / Jupyter 中作为内核运行
ipykernel>=6.0
