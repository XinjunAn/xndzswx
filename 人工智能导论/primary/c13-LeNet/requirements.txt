# === Jupyter 核心环境 ===
jupyter
notebook
ipykernel

# === 数据处理与基础计算 ===
numpy>=1.23.0
matplotlib>=3.5.0
# 虽然主要用 PyTorch，但保留 sklearn 是好习惯 (用于对比或数据处理)
scikit-learn>=1.2.0

# === 深度学习框架 (PyTorch) ===
# 这里的版本号仅作参考，实际安装时建议使用带有 CUDA 的命令
# 加上 --extra-index-url 是为了让 pip 知道去哪里找 GPU 版
torch>=2.0.0
torchvision>=0.15.0

# (可选) 用于更高级的进度条显示，跑训练循环时很帅
tqdm